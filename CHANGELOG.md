# تاریخچه تغییرات (Changelog)

## [v0.5.4] - 1405-05-16
### حل قطعی چالش‌های JS و Signature یوتیوب
- **تغییر موتور جاوااسکریپت به Deno:** اضافه شدن باینری رسمی `Deno` از طریق Multi-stage build در [Dockerfile](file:///home/unreal/projects/clip-srt-vps/Dockerfile) به عنوان موتور پیشنهادی و رسمی `yt-dlp` جهت رفع قطعی خطای `The page needs to be reloaded` و `n challenge solving failed`.
- **نصب پکیج کامل `yt-dlp[default]`:** ارتقای پیش‌نیاز در `requirements.txt` جهت نصب اکستراهای رسمی شامل `yt-dlp-ejs`، `pycryptodomex` و کتابخانه‌های حل چالش.

## [v0.5.3] - 1405-05-16
### عیب‌یابی و لاگینگ پیشرفته
- **فعال‌سازی حالت Verbose در `yt-dlp`:** افزودن `'verbose': True` و اتصال لاگر به لاگر اصلی سیستم (`logger`) جهت ثبت دقیق هدرها، کلاینت‌ها، موتورهای JS (Node.js) و تشخیص خطاهای استخراج مدیا در کنسول سرور.

## [v0.5.2] - 1405-05-16
### اصلاحات و بهینه‌سازی موتور دانلود
- **افزودن Node.js به محیط Docker:** نصب پکیج `nodejs` در داکرفایل سرور جهت حل خودکار و بومی چالش‌های رمزنگاری شده جاوااسکریپت یوتیوب (Signature / n challenge) توسط `yt-dlp`.
- **به‌روزرسانی `yt-dlp` به آخرین نسخه:** ارتقای حداقل نسخه مجاز پکیج `yt-dlp` در `requirements.txt` جهت همگام‌سازی با آخرین تغییرات یوتیوب.
- **حذف دستکاری‌های کلاینتی و بهینه‌سازی سلکتور فرمت:** حذف `player_client`های اجباری و تنظیم استخراج فرمت استاندارد همراه با تبدیل خودکار به MP4 با FFmpeg (`merge_output_format`).

## [v0.5.1] - 1405-05-16
### اصلاحات و بهبودها
- **بهینه‌سازی کلاینت‌های دانلود یوتیوب در `yt-dlp`:** اضافه شدن پارامترهای `extractor_args` با کلاینت‌های چندگانه (`android`, `ios`, `mweb`, `web`) و User-Agent استاندارد جهت جلوگیری از خطای `The page needs to be reloaded` و افزایش پایداری دانلودها.

## [v0.5.0] - 1405-05-16
### قابلیت‌های جدید (مدیریت کوکی‌ها از طریق داشبورد وب)
- **سیستم جامع مدیریت کوکی در داشبورد وب (`/dashboard`):** افزوده شدن کارت اختصاصی مدیریت کوکی‌ها با امکان انتخاب شیوه تنظیم (حالت عمومی یکپارچه یا تفکیک بر اساس پلتفرم یوتیوب و اینستاگرام).
- **پشتیبانی از Drag & Drop و بارگذاری فایل:** امکان کشیدن و رها کردن مستقیم فایل‌های `cookies.txt` روی بخش‌های مربوطه یا انتخاب فایل از حافظه دستگاه.
- **اندپوینت‌های امنیتی API کوکی:** پیاده‌سازی اندپوینت‌های RESTful امن (`GET/POST/DELETE /api/cookies`) با احراز هویت ادمین برای دریافت، ذخیره و پاک‌سازی کوکی‌ها.
- **معماری ذخیره‌سازی دوگانه (Hybrid Storage):** ذخیره‌سازی داده‌های کوکی در جدول `cookies_store` دیتابیس PostgreSQL (در صورت اتصال) و سوئیچ خودکار به ذخیره‌سازی محلی در فایل `.tmp/cookies_store.json` در حالت اجرای بدون دیتابیس.
- **اتصال مستقیم به موتور دانلود:** یکپارچه‌سازی `CookieManager` با سرویس `downloader` جهت استفاده آنی `yt-dlp` از آخرین کوکی‌های فعال بدون نیاز به ریستارت سرویس.

## [v0.4.4] - 1405-05-16
### قابلیت‌های جدید و بهینه‌سازی
- **پشتیبانی از متغیرهای محیطی کوکی یوتیوب (`YOUTUBE_COOKIES` و `COOKIES`):** اضافه شدن امکان تنظیم کوکی‌های احراز هویت یوتیوب به صورت رشته Base64 یا متن در متغیرهای محیطی جهت جلوگیری از محدودیت‌های دانلود در `yt-dlp`.
- **ترکیب و ادغام خودکار کوکی‌ها:** امکان خواندن و ترکیب هم‌زمان کوکی‌های `YOUTUBE_COOKIES`، `INSTAGRAM_COOKIES` و `COOKIES` در یک فایل موقت کوکی Netscape.
- **به‌روزرسانی تنظیمات و مستندات:** افزودن متغیرهای جدید به `render.yaml`، `.env.example` و مستندات انگلیسی/فارسی پروژه.

## [v0.4.3] - 1405-05-16
### مستندات
- **افزودن تصاویر پیش‌نمایش در README:** اضافه شدن اسکرین‌شات‌های محیط ربات تلگرام و داشبورد وب به فایل راهنمای گیت‌هاب جهت درک بهتر رابط کاربری.

## [v0.4.2] - 1405-05-16
### اصلاحات و بهبودها
- **تشخیص هوشمند ترک صوتی (Audio Track Detection):** اضافه شدن بررسی وجود استریم صدا با `ffprobe` قبل از اقدام به استخراج؛ در صورت ارسال ویدیوی بی‌صدا (Mute/GIF/No Audio)، ربات بدون کرش کردن خطای شفاف و فارسی همراه با نکات راهنما به کاربر نمایش می‌دهد.
- **بهینه‌سازی خروجی خطاهای FFmpeg:** افزودن فلگ‌های `-hide_banner` و `-loglevel error` و استخراج خطوط اصلی خطا از انتهای پیام، جهت جلوگیری از نمایش هدر بیلد FFmpeg در پیام‌های خطا.
- **بهبود دانلود استریم‌های ترکیبی در yt-dlp:** به‌روزرسانی انتخاب فرمت (`bestvideo+bestaudio/best`) جهت اولویت‌دهی به فایل‌های حاوی استریم کامل صوت و تصویر در پلتفرم‌های مختلف.

## [v0.4.1] - 1405-05-16
### اصلاحات
- **رفع خطای ساخت و اجرای Python 3.11 در Render:** برطرف کردن خطای `SyntaxError: f-string expression part cannot include a backslash` در فایل `main.py` با استخراج متغیرهای دکمه کپی به بیرون از عبارت f-string جهت سازگاری کامل با پایتون ۳.۱۱.

## [v0.4.0] - 1405-05-16
### قابلیت‌های جدید
- **بازطراحی داشبورد وب (`/dashboard`):** ادغام ۴ کارت آمار قبلی در ۱ کارت خلاصه جامع و افزودن کارت جدید «۳ کاربر برتر» با بیشترین تعداد درخواست.
- **جستجوی زنده در درخواست‌ها:** افزودن فیلد جستجوی لحظه‌ای در وب بر اساس شناسه کار، نام/آیدی کاربر، لینک ورودی، موضوع و پیام خطا.
- **نمایش و ذخیره موضوع کلیپ (Subject):** اضافه شدن ستون `subject` به جدول دیتابیس PostgreSQL و نمایش کامل موضوع کلیپ در داشبورد.
- **نمایش کامل داده‌ها و دکمه کپی:** برداشتن محدودیت طول متون در ستون‌های ورودی رسانه و موضوع کلیپ همراه با دکمه‌های کپی سریع برای استفاده مجدد.
- **نمایش تاریخ شمسی و منطقه زمانی کاربر:** تبدیل و نمایش زمان بر اساس منطقه زمانی مرورگر کاربر (Visitor Timezone) و قالب تاریخ شمسی (`1405-05-16 22:25:37`).
- **بهینه‌سازی برای موبایل:** طراحی کاملا واکنش‌گرا (Responsive) با فونت وزیرمتن و رابط کاربری مدرن dark mode.

## [v0.3.0] - 1405-05-16
### قابلیت‌های جدید
- **صفحه و مسیر `/about`:** پیاده‌سازی روت اختصاصی `/about` در FastAPI و دستور `/about` در تلگرام با نمایش نسخه دینامیک پروژه (`v0.3.0` Beta)، مشخصات توسعه‌دهنده و معماری سیستم.
- **نمایش مقاله Telegra.ph Instant View:** ادغام سرویس Telegra.ph برای نمایش ساختاریافته و سریع (Instant View) متون خروجی طولانی در تلگرام.
- **تولید موضوع هوشمند با AI:** تولید موضوع (Subject) به زبان فارسی با مدل هوش مصنوعی بر اساس متن زیرنویس و استفاده از آن در کپشن‌ها و نام‌گذاری فایل‌های خروجی `.txt` و `.mkv`.
- **گزینه‌های خروجی چندگانه:** ارائه ۴ گزینه پس از پیاده‌سازی (ویدیو زیرنویس‌دار softsub، متن کامل فارسی، متن خط به خط، و لینک instant view).
- **یکسان‌سازی نسخه پروژه:** به‌روزرسانی کلیه بخش‌های پروژه، روت‌ها و مستندات بر اساس ساختار نسخه Beta (`0.x.x`).

## [v0.2.0] - 1405-05-15
### قابلیت‌های جدید
- **پشتیبانی از دیتابیس PostgreSQL 18:** ذخیره‌سازی و پایداری وضعیت کارهای پردازش‌شده روی سرور Render.
- **به‌روزرسانی موتور ترجمه:** سوئیچ به مدل جدید Groq (`openai/gpt-oss-120b`) و امکان تنظیم سفارشی مدل.
- **پشتیبانی از چند کلید Gemini:** پیاده‌سازی مکانیزم الگوریتم Round-Robin برای توزیع بار بین چند کلید API گوگل جیمینای.

## [v0.1.1] - 1405-05-15
### اصلاحات
- بهینه‌سازی مدیریت کانکشن‌پول دیتابیس PostgreSQL روی سرویس رایگان Render.
- به‌روزرسانی قوانین پروژه و استانداردسازی سیستم ورژن‌دهی به صورت Beta (`0.x.x`).

## [v0.1.0] - 1405-05-14
### انتشار اولیه
- راه اندازی اولیه ربات تلگرام و سیستم استخراج صدا با FFmpeg و استخراج زیرنویس با Groq Whisper.