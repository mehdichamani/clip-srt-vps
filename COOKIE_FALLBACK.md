# 🍪 راهنمای مدیریت کوکی‌ها و مکانیزم Fallback در شرایط قطعی دیتابیس

این سند ساختار معماری پروژه **Clip SRT Bot** در مواجهه با قطعی دیتابیس (مانند انقضای پلن رایگان Render PostgreSQL) و نحوه پیکربندی پایدار کوکی‌ها برای پلتفرم‌های یوتیوب و اینستاگرام را شرح می‌دهد.

---

## ۱. معماری سیستم و مکانیزم Fail-Safe

در سرویس‌های [app/services/job_tracker.py](app/services/job_tracker.py) و [app/services/cookie_manager.py](app/services/cookie_manager.py)، معماری پروژه به صورت **Hybrid با مکانیزم Fallback خودکار** طراحی شده است:

* هنگام راه‌اندازی ربات (`lifespan`)، در صورت عدم دسترسی یا خطای اتصال به دیتابیس PostgreSQL، سیستم متوقف یا Crash نمی‌شود؛ بلکه فلگ `is_db_active = False` فعال شده و سیستم بلافاصله روی حالت‌های جایگزین (حافظه رم و فایل محلی موقت) سوئیچ می‌کند.

---

## ۲. وضعیت بخش‌های مختلف پس از قطعی یا انقضای دیتابیس

| بخش | وضعیت | توضیح دقیق رفتار سیستم |
| :--- | :---: | :--- |
| **کارکرد اصلی ربات تلگرام** | ✅ **سالم و فعال** | پردازش ویدیوها، دریافت لینک، دانلود با `yt-dlp`، استخراج زیرنویس، ترجمه هوش مصنوعی (Groq / Gemini) و تولید ویدیو زیرنویس‌چسبیده و صوت MP3 لیریکس‌دار کاملاً کار می‌کنند. |
| **رهگیری جاب‌ها (JobTracker)** | ⚠️ **فعال (In-Memory)** | جاب‌ها در حافظه موقت (RAM) نگهداری می‌شوند. لیست جاب‌ها تا زمان روشن بودن سرور در داشبورد وب نمایش داده می‌شود، اما با هر ری‌استارت یا Sleep وب‌سرویس، تاریخچه پاک می‌شود. |
| **آمار و داشبورد (`/admin`, `/stats`)** | ⚠️ **فعال با داده موقت** | داشبورد لود می‌شود، ولی تنها آمار جاب‌های انجام‌شده از زمان آخرین اجرای سرور تا کنون را نمایش می‌دهد. |
| **تغییر کوکی‌ها از وب‌داشبورد** | ⚠️ **موقت روی دیسک گذرا** | کوکی‌های جدید در فایل محلی موقت `.tmp/cookies_store.json` ذخیره می‌شوند. در پلن‌های رایگان ابری (مانند Render)، با ری‌استارت شدن سرور این فایل پاک می‌شود. |

---

## ۳. اولویت‌بندی بارگذاری کوکی‌ها در موتور دانلود

در [app/services/downloader.py](app/services/downloader.py)، بارگذاری کوکی‌ها با اولویت‌های زیر انجام می‌شود:

1. **اولویت اول (CookieManager):** کوکی‌های ثبت‌شده از طریق پنل وب (در صورت نبود دیتابیس، از فایل موقت محلی خوانده می‌شود).
2. **اولویت دوم (Environment Variables):** متغیرهای محیطی ثبت‌شده در تنظیمات سرور / رندر:
   - `COOKIES`
   - `YOUTUBE_COOKIES`
   - `INSTAGRAM_COOKIES`
3. **اولویت سوم (فایل‌های فیزیکی پروژه):** فایل‌های `cookies.txt` یا `combined_cookies.txt` در ریشه پروژه (در صورت وجود).

> [!WARNING]
> **نکته امنیتی و پایداری:** اگر کوکی‌های یوتیوب یا اینستاگرام را قبلاً فقط از طریق داشبورد وب ذخیره کرده بودید و در متغیرهای محیطی ثبت نشده بودند، پس از انقضای دیتابیس دیگر در دسترس نخواهند بود و دانلود لینک‌های نیازمند لاگین با خطا مواجه خواهد شد. بنابراین استفاده از متغیرهای محیطی Base64 توصیه اکید می‌شود.

---

## ۴. راهنمای گام‌به‌گام تنظیم کوکی‌های پایدار در متغیرهای محیطی

برای جلوگیری از مشکلات شکست خطوط (Multi-line Newline) در متغیرهای محیطی سرورهای ابری، محتوای فایل‌های کوکی استاندارد Netscape را به رشته تک‌خطی Base64 تبدیل کنید:

### گام ۱: استخراج فایل کوکی استاندارد Netscape
1. با افزونه مرورگر (مانند **Get cookies.txt LOCALLY**) کوکی‌های یوتیوب یا اینستاگرام را با فرمت Netscape در فایل‌های `youtube-cookies.txt` و `instagram-cookies.txt` خروجی بگیرید.
2. *(اختیاری برای یوتیوب)*: خطوط موقت تاریخچه بازدید شامل پیشوندهای `ST-*`، `CONSISTENCY` و `__Secure-Y*NID` را حذف کنید تا حجم به کوکی‌های احراز هویت کاهش یابد.

### گام ۲: تبدیل به رشته Base64

دستور متناسب با سیستم‌عامل خود را اجرا کنید:

#### در لینوکس / سرور مجازی (Linux / VPS):
```bash
# برای یوتیوب
base64 -w 0 youtube-cookies.txt

# برای اینستاگرام
base64 -w 0 instagram-cookies.txt
```

#### در مک‌او‌اس (macOS):
```bash
# برای یوتیوب
base64 -i youtube-cookies.txt

# برای اینستاگرام
base64 -i instagram-cookies.txt
```

#### در ویندوز (PowerShell):
```powershell
# برای یوتیوب
[Convert]::ToBase64String([IO.File]::ReadAllBytes("youtube-cookies.txt"))

# برای اینستاگرام
[Convert]::ToBase64String([IO.File]::ReadAllBytes("instagram-cookies.txt"))
```

### گام ۳: ثبت در متغیرهای محیطی Render

1. وارد پنل مدیریت [Render Dashboard](https://dashboard.render.com) شده و وب‌سرویس خود را انتخاب کنید.
2. به بخش **Environment** رفته و روی **Add Environment Variable** کلیک کنید:
   - **کلید (Key):** `YOUTUBE_COOKIES` ➔ **مقدار (Value):** رشته Base64 تولید شده یوتیوب
   - **کلید (Key):** `INSTAGRAM_COOKIES` ➔ **مقدار (Value):** رشته Base64 تولید شده اینستاگرام
   - *(یا کلید عمومی)*: `COOKIES` برای کوکی‌های ترکیبی Netscape
3. روی **Save Changes** کلیک کنید. سیستم پس از بازنشانی خودکار، کوکی‌ها را بارگذاری می‌کند.

---

## ۵. راه‌اندازی مجدد دیتابیس (اختیاری)

در صورت تمایل به ذخیره‌سازی دائمی تاریخچه جاب‌ها و لاگ‌ها:
* می‌توانید در Render یک دیتابیس PostgreSQL رایگان جدید بسازید (یا از سرویس‌های ابری رایگان مثل Neon یا Supabase استفاده کنید).
* مقدار `DATABASE_URL` را در بخش متغیرهای محیطی وب‌سرویس قرار دهید. سیستم جداول مورد نیاز را به صورت خودکار ایجاد و متصل خواهد کرد.
